#include <stdio.h>

/**
 *main - program that prints all nomers of base 10 starting from 0
 *Return: 0 exit code of the program if successful
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
